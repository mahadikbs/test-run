name: Hello saurabh
on:
  push:
    branches:
      - main

jobs:
  Hello:
    runs-on: ubunut-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: execute script
        run: |
          chmod 777 *
          ./test.sh  